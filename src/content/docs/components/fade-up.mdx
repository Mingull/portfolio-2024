---
    title: Fade Up
    summary: A simple fade up component that can be used to animate elements as they enter the viewport.
---

The `FadeUp` component is a simple React component that can be used to animate elements as they enter the viewport. It uses the `Framer Motion` library to create a smooth fade up animation.
